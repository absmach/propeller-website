---
title: Manager
description: Central service for task management and proplet coordination in Propeller
---

The Manager is Propeller's central orchestration service. It coordinates the complete lifecycle of WebAssembly workloads across distributed proplets—from task creation and scheduling through execution tracking and result collection.

For a quick start with basic task operations, see the [Getting Started](/docs/getting-started) guide. For advanced workflow patterns, see [Task Scheduling](/docs/task-scheduling).

## Why the Manager

### Central Coordination

Distributed edge computing requires a coordinator that understands the state of all nodes and workloads. The Manager provides:

- **Single point of control**: One API to manage tasks across all proplets
- **State persistence**: All task and proplet state survives restarts
- **Scheduling intelligence**: Automatic proplet selection based on availability and load
- **Execution tracking**: Real-time visibility into task progress and results

### Decoupled Communication

The Manager acts as an intermediary between HTTP clients and MQTT-connected proplets:

![Manager Communication Flow](./images/manager-communication-flow.svg)

This separation allows clients to use familiar REST APIs while proplets communicate over lightweight MQTT—ideal for constrained edge devices.

For the full system architecture and component overview, see the [Architecture](/docs/architecture) documentation.

## Running the Manager

### Using Docker Compose

The recommended way to run the Manager is with Docker Compose:

```bash
cd propeller
docker compose -f docker/compose.yaml --env-file docker/.env up -d
```

Verify the Manager is running:

```bash
curl http://localhost:7070/health
```

Your output should look like this:

```json
{"instance_id":"a1b2c3d4-e5f6-7890-abcd-ef1234567890"}
```

### Configuration

The Manager is configured through environment variables. These are the most commonly used:

#### Core Settings

| Variable | Description | Default |
| -------- | ----------- | ------- |
| `MANAGER_LOG_LEVEL` | Log level (`debug`, `info`, `warn`, `error`) | `info` |
| `MANAGER_INSTANCE_ID` | Unique identifier for this manager instance | Auto-generated UUID |

#### MQTT Connection

The Manager communicates with proplets over MQTT using the [SuperMQ](https://github.com/absmach/supermq) message broker. SuperMQ provides secure, multi-tenant messaging with built-in authentication and authorization—ensuring that only authorized components can publish and subscribe to Propeller's control channels.

| Variable | Description | Default |
| -------- | ----------- | ------- |
| `MANAGER_MQTT_ADDRESS` | MQTT broker URL | `tcp://localhost:1883` |
| `MANAGER_MQTT_QOS` | Quality of Service level (0, 1, or 2) | `2` |
| `MANAGER_MQTT_TIMEOUT` | Connection and operation timeout | `30s` |
| `MANAGER_DOMAIN_ID` | SuperMQ domain identifier | From `config.toml` |
| `MANAGER_CHANNEL_ID` | SuperMQ channel identifier | From `config.toml` |
| `MANAGER_CLIENT_ID` | MQTT client ID for authentication | From `config.toml` |
| `MANAGER_CLIENT_KEY` | MQTT client key for authentication | From `config.toml` |

**QoS Levels:**
- `0` (At most once): Fire and forget—fastest but messages may be lost
- `1` (At least once): Guaranteed delivery but may have duplicates
- `2` (Exactly once): Guaranteed single delivery—recommended for Propeller

##### Provisioning with config.toml

The `MANAGER_DOMAIN_ID`, `MANAGER_CHANNEL_ID`, `MANAGER_CLIENT_ID`, and `MANAGER_CLIENT_KEY` values come from SuperMQ provisioning. The `propeller-cli provision` command automates this setup by:

1. Authenticating with your SuperMQ instance
2. Creating a domain (logical isolation boundary)
3. Creating clients (Manager, Proplets, Proxy)
4. Creating a channel (shared communication topic)
5. Connecting all clients to the channel with publish/subscribe permissions
6. Writing credentials to `config.toml`

Run the provisioning wizard:

```bash
propeller-cli provision
```

The wizard prompts for SuperMQ credentials and generates a `config.toml` file:

```toml
# SuperMQ Configuration

[manager]
domain_id = "73cc3c8f-2c6c-4c08-903b-f1f426d5d9bf"
client_id = "47c7fcf5-2d9a-41e6-8e0c-97303736019e"
client_key = "26975130-4cd7-4791-8050-c1698ac31e10"
channel_id = "9856d353-88d3-4eb6-82bd-117eadf69c74"

[proplet]
domain_id = "73cc3c8f-2c6c-4c08-903b-f1f426d5d9bf"
client_id = "ec182939-7940-4b25-869e-47b245ddec09"
client_key = "21076163-f7b9-439e-94b9-f17f69ce3d28"
channel_id = "9856d353-88d3-4eb6-82bd-117eadf69c74"

[proxy]
domain_id = "73cc3c8f-2c6c-4c08-903b-f1f426d5d9bf"
client_id = "ec182939-7940-4b25-869e-47b245ddec09"
client_key = "21076163-f7b9-439e-94b9-f17f69ce3d28"
channel_id = "9856d353-88d3-4eb6-82bd-117eadf69c74"
```

Each section contains:

| Field | Purpose |
| ----- | ------- |
| `domain_id` | SuperMQ domain for multi-tenant isolation |
| `client_id` | Unique identifier for MQTT authentication |
| `client_key` | Secret key for MQTT authentication |
| `channel_id` | Shared channel for Manager-Proplet communication |

The Manager, Proplets, and Proxy all share the same `domain_id` and `channel_id` but have unique `client_id`/`client_key` pairs. This allows them to communicate securely on a shared channel while maintaining distinct identities.

When starting Docker Compose, the `docker/.env` file references these values, which are loaded from `config.toml`. You can also set them directly as environment variables to override the file.

#### HTTP Server

| Variable | Description | Default |
| -------- | ----------- | ------- |
| `MANAGER_HTTP_HOST` | HTTP listen host | `""` (all interfaces) |
| `MANAGER_HTTP_PORT` | HTTP listen port | `7070` |
| `MANAGER_HTTP_SERVER_CERT` | TLS certificate file path | `""` |
| `MANAGER_HTTP_SERVER_KEY` | TLS private key file path | `""` |

#### Storage Backend

Select your storage backend with `MANAGER_STORAGE_TYPE`:

| Backend | Best For | Additional Config |
| ------- | -------- | ----------------- |
| `memory` | Development and testing | None (state lost on restart) |
| `sqlite` | Single-node or edge deployments | `MANAGER_SQLITE_PATH` |
| `postgres` | Production with durability requirements | PostgreSQL connection vars |
| `badger` | High-performance edge deployments | `MANAGER_BADGER_PATH` |

**SQLite configuration:**

```bash
export MANAGER_STORAGE_TYPE=sqlite
export MANAGER_SQLITE_PATH=./propeller.db
```

**PostgreSQL configuration:**

```bash
export MANAGER_STORAGE_TYPE=postgres
export MANAGER_POSTGRES_HOST=localhost
export MANAGER_POSTGRES_PORT=5432
export MANAGER_POSTGRES_USER=propeller
export MANAGER_POSTGRES_PASS=propeller
export MANAGER_POSTGRES_DB=propeller
export MANAGER_POSTGRES_SSLMODE=disable
```

#### Observability

| Variable | Description | Default |
| -------- | ----------- | ------- |
| `MANAGER_OTEL_URL` | OpenTelemetry collector URL | `""` (disabled) |
| `MANAGER_TRACE_RATIO` | Fraction of requests to trace (0.0–1.0) | `0` |

#### Federated Learning

| Variable | Description | Default |
| -------- | ----------- | ------- |
| `COORDINATOR_URL` | External FL Coordinator service URL | `""` (FL disabled) |

When `COORDINATOR_URL` is not set, the Manager logs a warning at startup and FL endpoints return errors.

## Task Operations

Tasks are the fundamental unit of work in Propeller. Each task represents a WebAssembly module to execute on a proplet.

### Creating a Task

Create a task by specifying its name and either an OCI image URL or a base64-encoded Wasm binary:

```bash
curl -X POST "http://localhost:7070/tasks" \
-H "Content-Type: application/json" \
-d '{
  "name": "hello-world",
  "image_url": "ghcr.io/myorg/hello:latest"
}'
```

Your output should look like this:

```json
{
  "id": "9fa47dfa-d96a-4fb5-a3e5-07aa9abdb841",
  "name": "hello-world",
  "kind": "standard",
  "state": 0,
  "image_url": "ghcr.io/myorg/hello:latest",
  "daemon": false,
  "encrypted": false,
  "start_time": "0001-01-01T00:00:00Z",
  "finish_time": "0001-01-01T00:00:00Z",
  "created_at": "2026-03-01T12:00:00.000000000Z",
  "updated_at": "0001-01-01T00:00:00Z",
  "priority": 50
}
```

The task is created in state `0` (Pending), waiting to be started.

### Task Fields

Tasks support various fields for different execution scenarios:

| Field | Type | Description |
| ----- | ---- | ----------- |
| `name` | string | Human-readable task name (required) |
| `image_url` | string | OCI registry URL for the Wasm binary |
| `file` | []byte | Base64-encoded Wasm binary (alternative to `image_url`) |
| `cli_args` | []string | Command-line arguments passed to the Wasm module |
| `inputs` | []uint64 | Input values for compute tasks |
| `env` | map | Environment variables for the Wasm module |
| `daemon` | bool | Run continuously until explicitly stopped |
| `encrypted` | bool | Wasm binary is encrypted (requires KBS) |
| `kbs_resource_path` | string | KBS resource path for encrypted binaries (TEE only) |
| `priority` | int | Dispatch priority (0–100, default 50) |
| `schedule` | string | Cron expression for scheduled execution |
| `timezone` | string | Timezone for scheduled tasks (default UTC) |
| `is_recurring` | bool | Re-run according to schedule |

### Starting a Task

Once created, start the task to schedule it for execution:

```bash
curl -X POST "http://localhost:7070/tasks/9fa47dfa-d96a-4fb5-a3e5-07aa9abdb841/start"
```

Your output should look like this:

```json
{"started": true}
```

The Manager:
1. Selects an available proplet using round-robin scheduling
2. Publishes a start command to the proplet's MQTT topic
3. Updates the task state to `1` (Scheduled)

### Checking Task Status

Monitor task progress by fetching its current state:

```bash
curl "http://localhost:7070/tasks/9fa47dfa-d96a-4fb5-a3e5-07aa9abdb841"
```

Your output should look like this:

```json
{
  "id": "9fa47dfa-d96a-4fb5-a3e5-07aa9abdb841",
  "name": "hello-world",
  "state": 3,
  "proplet_id": "a95517f9-5655-4cf5-a7c8-aa00290b3895",
  "results": "Hello, World!",
  "start_time": "2026-03-01T12:00:05.000000000Z",
  "finish_time": "2026-03-01T12:00:05.500000000Z"
}
```

State `3` indicates the task completed successfully.

### Task States

| Value | Name | Description |
| ----- | ---- | ----------- |
| `0` | Pending | Task created, not yet started |
| `1` | Scheduled | Task assigned to a proplet |
| `2` | Running | Task is executing on a proplet |
| `3` | Completed | Task finished successfully |
| `4` | Failed | Task encountered an error |
| `5` | Skipped | Task skipped (workflow condition not met) |
| `6` | Interrupted | Task was stopped externally |

### Uploading Wasm Directly

Instead of using an OCI registry, upload a Wasm file directly:

```bash
# Create task without image_url
curl -X POST "http://localhost:7070/tasks" \
-H "Content-Type: application/json" \
-d '{"name": "my-module"}'

# Upload the Wasm binary (max 100 MB)
curl -X PUT "http://localhost:7070/tasks/{task_id}/upload" \
-F "file=@./build/my-module.wasm"
```

## Proplet Management

Proplets are the edge runtimes that execute tasks. The Manager tracks their availability through MQTT heartbeats.

### Listing Proplets

```bash
curl "http://localhost:7070/proplets?limit=10"
```

Your output should look like this:

```json
{
  "proplets": [
    {
      "id": "a95517f9-5655-4cf5-a7c8-aa00290b3895",
      "alive_history": ["2026-03-01T12:00:00Z", "2026-03-01T12:00:10Z"]
    }
  ],
  "total": 1,
  "offset": 0,
  "limit": 10
}
```

### Proplet Liveliness

A proplet is considered alive if it sent a heartbeat within the last 10 seconds. The Manager stores the last 10 heartbeat timestamps for debugging connectivity issues.

Proplets send heartbeats to the MQTT topic:

```
m/{domain_id}/c/{channel_id}/control/proplet/alive
```

Dead proplets are excluded from scheduling—tasks are only dispatched to alive proplets.

## Jobs

Jobs group multiple tasks together and execute them with a common strategy. Unlike workflows, job tasks don't have dependencies—they run based on the execution mode.

### Creating a Job

```bash
curl -X POST "http://localhost:7070/jobs" \
-H "Content-Type: application/json" \
-d '{
  "name": "batch-process",
  "execution_mode": "sequential",
  "tasks": [
    {"name": "step-1", "image_url": "docker.io/myorg/step1:v1"},
    {"name": "step-2", "image_url": "docker.io/myorg/step2:v1"},
    {"name": "step-3", "image_url": "docker.io/myorg/step3:v1"}
  ]
}'
```

Your output should look like this:

```json
{
  "job_id": "b2c3d4e5-f6a7-8901-bcde-f23456789012",
  "tasks": [
    {"id": "task-1-id", "name": "step-1", "state": 0},
    {"id": "task-2-id", "name": "step-2", "state": 0},
    {"id": "task-3-id", "name": "step-3", "state": 0}
  ]
}
```

### Execution Modes

| Mode | Behavior |
| ---- | -------- |
| `parallel` | All tasks start simultaneously on different proplets |
| `sequential` | Tasks run one at a time in order; first failure stops the job |
| `configurable` | Mode is read from `JOB_EXECUTION_MODE` environment variable at runtime |

### Starting and Stopping Jobs

```bash
# Start all tasks in the job
curl -X POST "http://localhost:7070/jobs/{job_id}/start"

# Stop all running tasks in the job
curl -X POST "http://localhost:7070/jobs/{job_id}/stop"
```

Jobs cannot be updated or deleted once created. To modify a job, create a new one with the desired configuration.

## Workflows

Workflows use DAG-based ordering to express dependencies between tasks. See [Task Scheduling](/docs/task-scheduling) for detailed workflow documentation.

### Creating a Workflow

```bash
curl -X POST "http://localhost:7070/workflows" \
-H "Content-Type: application/json" \
-d '{
  "tasks": [
    {"name": "fetch-data", "image_url": "docker.io/myorg/fetch:v1"},
    {
      "name": "process",
      "image_url": "docker.io/myorg/process:v1",
      "depends_on": ["<fetch-data-task-id>"],
      "run_if": "success"
    }
  ]
}'
```

The `run_if` field controls conditional execution:

| Value | Behavior |
| ----- | -------- |
| `success` | Run only if all dependencies completed successfully (default) |
| `failure` | Run only if any dependency failed |
| `always` | Always run regardless of dependency outcome |

## Scheduling

The Manager includes a built-in cron scheduler for recurring tasks.

### Creating a Scheduled Task

```bash
curl -X POST "http://localhost:7070/tasks" \
-H "Content-Type: application/json" \
-d '{
  "name": "hourly-report",
  "image_url": "docker.io/myorg/report:v1",
  "schedule": "0 * * * *",
  "timezone": "UTC",
  "is_recurring": true
}'
```

The scheduler checks for due tasks every 60 seconds.

### Scheduling Algorithms

The Manager uses two scheduling mechanisms:

| Mechanism | Purpose | Behavior |
| --------- | ------- | -------- |
| **Round Robin** | Proplet selection | Cycles evenly across alive proplets |
| **Priority Ordering** | Task dispatch order | Higher priority (0–100) dispatched first; ties broken by creation time |

Priority determines which task gets dispatched next. Round Robin determines which proplet receives it.

## Observability

### Prometheus Metrics

The Manager exposes Prometheus metrics at `/metrics`:

```bash
curl http://localhost:7070/metrics
```

Metrics include request counts, latencies, and task state distributions.

### OpenTelemetry Tracing

Enable distributed tracing by setting:

```bash
export MANAGER_OTEL_URL=http://jaeger:4318
export MANAGER_TRACE_RATIO=0.1  # Sample 10% of requests
```

### Middleware Stack

Every request passes through three middleware layers:

1. **Logging**: Logs method calls with duration and errors
2. **Metrics**: Records call counts and latencies for Prometheus
3. **Tracing**: Adds OpenTelemetry spans when configured

## Data Flow

### Task Execution Flow

```
1. Client → POST /tasks        → Manager creates task in Pending state
2. Client → POST /tasks/{id}/start → Manager selects proplet, publishes MQTT command
3. Manager → MQTT "start"      → Proplet receives task
4. Proplet → Executes Wasm     → Produces results
5. Proplet → MQTT "results"    → Manager receives results
6. Manager → Updates state     → Task moves to Completed/Failed
```

### Proplet Registration Flow

```
1. Proplet starts             → Publishes discovery to control/proplet/create
2. Manager receives           → Records proplet ID and channel
3. Proplet sends heartbeats   → Every few seconds to control/proplet/alive
4. Manager tracks liveliness  → Proplet eligible for scheduling if heartbeat < 10s old
```

## API Reference

For detailed request/response schemas and examples, see the [API Reference](/docs/api).

| Resource | Endpoints |
| -------- | --------- |
| Tasks | `POST`, `GET`, `PUT`, `DELETE /tasks`, `/tasks/{id}/start`, `/tasks/{id}/stop` |
| Proplets | `GET`, `DELETE /proplets`, `/proplets/{id}/metrics` |
| Jobs | `POST`, `GET /jobs`, `/jobs/{id}/start`, `/jobs/{id}/stop` |
| Workflows | `POST /workflows` |
| Federated Learning | `/fl/experiments`, `/fl/task`, `/fl/update`, `/fl/rounds` |
| System | `GET /health`, `GET /metrics` |
